# TAPE · 5 SEPTEMBER (afternoon) → 6 SEPTEMBER 2026

**Prepend to `docs/THINGDADDY_PHASE_LOG.md` above the 5 September morning entry.**
Numbers assigned on paste. Companion files in `docs/tape/`.

---

# PART A · CORRECTIONS TO THE 5 SEPTEMBER MORNING ENTRY — read these first

**C1 · MedFare is not a containment finding.** The morning tape records `081708901` (MedFare LLC)
as a key filed inside Diazyme's licence `0817089`. **`0817089` was never a licence key.** Verified by
GS1, read by hand 5 Sep 17:26 BKK, screens filed at `population/standards/captures/IMG_6519.PNG`
and `IMG_6520.PNG`: Diazyme holds **`081708902`** and **`081730202`**, both nine-digit GS1 Company
Prefixes, GS1 US. The GS1 encoder splits both at nine. The GCP Length Table said `0817 → 9` all
along. **`081708901` and `081708902` are sibling nine-digit licences.** There is no containment.
The check (C1/C2 in `td_audit.py`) is correct; the interpretation was wrong. **The morning entry
stands as written; this supersedes it.**

**C2 · The "hardcoded 081708902" accusation withdrawn.** The P1 claim reading `081708902` graded
`v` with a `gcp_cut` provenance sentence was **correct all along** — it is the licence key, and its
derivation reproduces. What was wrong was the party row beside it.

---

# PART B · CRASH-TEST DUMMY #2 — the correction that installed the next defect

Full record: `docs/tape/LESSON_crash_test_dummy_2_2026-09-05.md`. The chain, dated:

```
20 Jul   the fix for the 0614141 ghost writes diazyme.com → 0817089 into
         verified_prefixes.json labelled "GEPIR-verified".
         GEPIR HAD BEEN RETIRED NINETEEN MONTHS. No authority was consulted.
22 Jul   giai:0817089.1 and .2 TYPED, not minted. "One changed digit per
         machine is the whole differentiator."
 7 Aug   the conflict raised. Ruled: "the authority decides, we do not derive.
         081708902 on hold until VBG states it by hand." THE READ WAS NEVER DONE.
 5 Sep   td_rederive.py fails its positive control (expected 0817089, table
         returns 081708902), REFUSES TO JUDGE 12,047 claims, and the refusal
         forces the read.
```

**Why it survived seven weeks: `giai:0817089.2` rendered without its dot IS `081708902`.** The
error validated itself. Only re-derivation could find it; inspection never would.

**Seven rules proposed for ratification (§6 of the lesson):** a correction can install the next
defect · a source name is a claim true on the day written · a self-validating error is the hardest
class · the separator must travel · no EPC key is ever hand-written · a control before an
experiment, failing it is fatal · an open ruling is an open wound.

---

# PART C · THE FROZEN SPINE — 976 of 1,127

The re-root (`reroot_diazyme.py`) was refused by `node_urn_epc_valid` on a **no-op update** to node
90799. **The constraint is NOT VALID: it never checked the existing rows and enforces on every
write.** 976 of the spine's 1,127 EPC URNs fail it — `urn:epc:id:sgtin:00817089020016` is a GTIN-14
wearing a serialised scheme name, no dots, no serial. **The 974-row defect ruled 7 August, unfixed
and invisible until something tried to write.** Every one of those rows is read-only and nothing
said so.

**Audit checks A1 rescoped and A4 added.** A NOT VALID constraint does not skip a check; it
conceals a population. The re-root is blocked, correctly, behind: fix the scheme at the producer
→ validate → re-mint through the engine → move the root.

---

# PART D · 6 SEPTEMBER — RULINGS

**R-a · No prefix, no production.** KJ, verbatim: *"no prefix no production.. it should be nothing
leaves DB1 without a clean prefix."*

**R-b · DB1 → DB2 → DB3, no exceptions.** *"i want all future work to follow our promotion rules
db1 to db2 to db3."* Six write routes on `server.js` connect to DB3 (`/party/:id/edge`, `/ingest`,
`/gate/:id`, `/claim/:id`, `/party/:id/asset`, `/asset/:id/carrier`). **`/ingest` violates it
outright.** The other five are runtime-plane writes on rooted records — **the plane question is
KJ's ruling, pending.**

**R-c · GDSN is a later lane.** Not another word on it today. Left in place.

**R-d · Multi-prefix parties are the 1,794 no-prefix staging rows** — `party.prefix` is scalar;
the prefixes are in `company_prefix`. Not orphans.

---

# PART E · 6 SEPTEMBER — FINDINGS

**E1 · The mirror.** `td_mirror.py` checks every artefact the record claims was built against disk
and git. **80 claimed · 20 solid · 14 dark · 10 in multiple places · 36 lost.** Only 10 of 92
claimed artefacts were ever uploaded to the project. *Delivered-to-chat is not saved-to-disk;
saved-to-disk is not committed.*

**E2 · The 22 July team package** — `~/Downloads/thingdaddy 2/`, 22 files, commit `3b70d44`, no
remote, never applied to any database, its Python nowhere in the repo. Reviewed file by file as
**future inputs, not past work**:

```
001_scaffold.sql     5 constraints production lacks (installed today, migration 012)
002_admit.sql        classify() — INV-4 as code, exists nowhere else. admit() — the
                     single identity door; production has a FACT door (gate_admit)
                     and no THING door.
003_promote.sql      _is_named_actor() IN THE DATABASE, not a CLI flag. Root-first.
004_sandbox.sql      migrate_to_real() — the claim → re-root, atomic, through
                     admit(). THE MISSING OPERATION. Reference for G1.
harvest_extract.py   one lane missing in production: Digital Link /01/ as a
                     GTIN source (read DL, store the GTIN — per the DL rule)
load_router.py       superseded by the promoter
project_thingsite.py properly migrated, 136 → 238 lines
```

**Nothing pushed. The package stays where it is until each idea is built fresh or struck.**

**E3 · Two engines.** `POST /party/:id/asset` mints through `mint_cli.py` → `td_engine_ref.py`
(the stand-in), not `td_gs1_engine.py` (the reference). Route has minted zero rows. The engine has
`giai · gsrn · gdti · cpid · install · transfer · admit · capitalize · falsify` and **no cut
function** — `gcp_cut` lives in loaders. **One verb reachable, eight not.** Prior claim "none
reachable" was wrong.

**E4 · Migration 012 installed.** `node.accounting_role` · `party_event.authority` ·
`party_event.ruleset_version` · `party_event_has_actor` (validated, 0 fail) ·
`node_verified_has_urn` (NOT VALID, **20 fail**: the gepir/seed verified PGLN nodes — Pfizer,
Waters, Beckman, Tecan, Labcorp Genetics, Leica ×2, Eppendorf, Lilly — verified with no URN).
A1 now counts 4. The fourth is the only one that lists its own failing rows.

**E5 · The promote dry run, full volume** — `docs/promote/dry_2026-09-06.txt`:

```
QUEUE 23,910 · ADMITTED 15,502 · UNCHANGED 138 · REFUSED 8,270
  party/holds-other-prefix   6,958   the scalar party.prefix column. Under
                                     prefix-as-key these are 6,958 rows, no
                                     conflict.
  prefix/held-elsewhere      1,307   two parties, one prefix. Walmart (party
                                     4081376, holding a GS1 India prefix) claims
                                     twelve held by ASO, Trividia, Ranir, B&L,
                                     Sam's Club. Private brands or contract
                                     manufacturers' prefixes — ONLY VbG says which.
  gln/rooting-conflict           5   Dako: 5700571–5 (GS1 Denmark) vs GLN
                                     0685346000008 (US). Multi-MO. Correct refusal.
```

**The machine works.** DemeTECH (`0652927`): 1 party, 1 prefix, 13,903 devices, admitted
candidate, `licence_type-not-read`. **Apply is Monday.**

**E6 · Days 1–4 of Friday's plan, measured.** Done before today: tool inventory (the mirror),
party DDL, 13 `harvest_company` constraints, DemeTECH staged. **Absent and built today or tonight:**
`promote_harvest_to_stage` (W2, four gates + slot vocabulary), the GUDID time-axis re-extract
(night job). `p25` is a name-match loader, not a gated promoter.

**E7 · W1's Shimadzu finding — IP.** The CBM-40 CMD: *"can be removed, reinitialized, and reused
attached to a new column."* **A carrier the maker designs to be re-pointed at a different asset.**
Custody runs asset-moves-identity-follows; here the carrier persists and the asset changes
beneath it. A graph treating a carrier read as identity inherits the previous column's history.
And Column Manager IDs are free text — `0001`, `Demo_01` — **no check digit, no namespace, no
issuing authority.** To Jane.

**E8 · Branch collision.** `git checkout -b day/2026-09-06` in the shared worktree switched W1
mid-task. Commits `daa0aa4`, `f7373ad` safe on `commit/ontology-builder`. **Fix: one worktree per
window** — `~/thingdaddy-w2`, `-w3`, `-w4` created. **Standing rule: never switch branches in a
worktree another window is using.**

---

# PART F · COMMITS ON `day/2026-09-06`

```
5047b4b   stabilise: screens tracked, ignore rule narrowed, mirror in
b6752c5   schema: migration 012, five constitutional constraints
34864f9   promote: full-volume dry run, refusal report
42be011   merge origin/main — canon and phase log onto day/
```

---

# PART G · OWED — named, not hidden

```
the plane ruling            do the five runtime routes obey DB1→DB2→DB3?
the P2 ruling               Drivers (9 Jul, DB3, book) or Documents (26 Jul)?
ACME's prefix               952 range per GenSpecs §1.2.3.5, NOT 0614141
the 20 verified-no-URN      mint the PGLNs through the engine, or demote
the 12 authority reads      VbG, 20 minutes, screens to captures/
the 36 lost artefacts       decide, rebuild, or strike — each one
the re-root                 scheme fix → validate → re-mint → move
migrate_to_real()           tomorrow. G1 step 1.
```

*Two crash-test dummies now. The second was planted by the fix for the first. The machine that
found it refused to trust itself.*
