# THINGDADDY_PHASE_LOG.md — the journal (the tape)

**Append‑only. Newest on top. The record belongs to KJ.**
*Companion to `00_READ_ME_FIRST` (the tape) and `THINGDADDY_CANONICAL_FOUNDATION` (the law). This is the journal: what was assumed, what is true, which test/source enforces it.*

> **RECONSTRUCTION NOTICE — 2026‑08‑02.** The canonical Phase Log went missing from the project doc list, so daily grounded findings stopped committing (the nightly sweeps flagged this as ACUTE for days). This file **resumes accrual from 2026‑08‑02**. The canonical original — entries **N1…N27** and the full correction history — lives on the **Mac** (`claude/THINGDADDY_PHASE_LOG.md`); when located, **PREPEND the originals above the corrections tape; do not overwrite** this resume block. Everything below is grounded and sourced; nothing minted, nothing promoted, R4 intact.

---

## TAPE ENTRIES — filed under `docs/tape/` · newest first

*Each day's tape is filed whole, as written. The paragraphs below are pointers, not the record. Numbering on the tapes is still unresolved; numbers are assigned on paste.*

**2026‑09‑06 · [TAPE · no prefix, no production; the spine was frozen](tape/TAPE_2026-09-06.md).** Covers 5 September afternoon through 6 September. Part A corrects the 5 September morning entry: MedFare's `081708901` is not a key filed inside Diazyme's licence, because `0817089` was never a licence key. Verified by GS1, read by hand with screens filed under `population/standards/captures/`, shows Diazyme holds `081708902` and `081730202`, both nine‑digit GS1 US prefixes, so `081708901` and `081708902` are sibling licences and there is no containment; the check was right, the interpretation was wrong, and the “hardcoded 081708902” accusation is withdrawn. Part B dates the crash‑test dummy #2 chain: 20 July the fix writes a GEPIR label nineteen months after GEPIR retired, 22 July two GIAIs typed by hand, 7 August the hold ruled and the read never done, 5 September `td_rederive.py` fails its positive control and forces the read. Part C: the re‑root was refused by `node_urn_epc_valid` on a no‑op update; the constraint is NOT VALID, never checked existing rows, and 976 of the spine's 1,127 EPC URNs fail it, GTIN‑14s wearing a serialised scheme name, read‑only since the 7 August ruling with nothing saying so. Audit A1 rescoped and A4 added: a NOT VALID constraint conceals a population. Rulings: no prefix, no production, nothing leaves DB1 without a clean prefix; all future work follows DB1 → DB2 → DB3, with `/ingest` violating it outright and the five runtime‑plane routes pending KJ's plane ruling; GDSN is a later lane; the 1,794 no‑prefix staging rows are multi‑prefix parties, not orphans. Findings: the mirror counts 80 claimed artefacts, 20 solid, 14 dark, 10 in multiple places, 36 lost, and delivered‑to‑chat is not saved‑to‑disk; the 22 July team package reviewed file by file as future inputs, nothing pushed, with `migrate_to_real()` named the missing operation and the reference for G1; two engines, one verb reachable and eight not, `gcp_cut` living in loaders; migration 012 installed, `node_verified_has_urn` NOT VALID with 20 verified PGLN nodes failing it; the full‑volume promote dry run at 23,910 queued, 15,502 admitted, 8,270 refused, the machine works on DemeTECH and apply is Monday; W1's Shimadzu CBM‑40 finding, a carrier the maker designs to be re‑pointed at a different asset with free‑text Column Manager IDs, to Jane as IP; the branch collision fixed with one worktree per window and the standing rule never to switch branches in a worktree another window is using. Four commits on `day/2026-09-06`. Eight items owed, named in Part G: the plane ruling, the P2 ruling, ACME's prefix, the 20 verified‑no‑URN nodes, the 12 authority reads, the 36 lost artefacts, the re‑root, and `migrate_to_real()`.

**2026‑09‑05 · [TAPE · the network told us three times](tape/TAPE_2026-09-05.md).** KJ's ruling: the registrar reports, the owner decides; we state the finding, never the cause. The data spoke three times. A `bun` server started 22 August had been answering `/health` for two weeks while the screens called routes it did not have, so a port that answers is not a build that serves, and BUILD DRIFT becomes a named watcher. A search for Diazyme's `0817089` returned MedFare holding `081708901` with GLN `0817089010017`, a key issued inside another company's licence and read as its own root; the fix is containment (`issued-inside`) shown separately, and four causes that look identical are never distinguished. The prefix search dropped the unrooted Diazyme row under a banner asserting completeness. Part C names the Google "was this you?" mechanic as the model for owner authorisation; Part D sets out the four‑watcher fleet (containment, completeness, custody gap, build drift) and its shared shape; Part E audits GoDaddy's catalogue into free mirror versus paid vigilance with the rule that nothing making the register trustworthy is ever charged for. Shipped: canon and phase log merged to main for the first time, the test suite invocable (honest number 1 pass, 12 fail, fix the fixtures never the constraint), the dev seed proven, `demo_up.sh` with a route check. Six files found living in exactly one place, one lost. Eight Claude corrections, all the same cause: reasoning from a file or a memory instead of reading what was there. Three IP flags routed to Jane. GAS stays at 3.

**2026‑09‑05 · [LESSON · crash‑test dummy #2, the correction that installed the next defect](tape/LESSON_crash_test_dummy_2_2026-09-05.md).** The fix for the first crash‑test dummy (20 July, the `0614141` eviction) rewrote `verified_prefixes.json` to a single entry, `diazyme.com → 0817089`, labelled GEPIR‑verified nineteen months after GEPIR was retired. Two GIAIs were then typed by hand on that root. On 5 September Verified by GS1, read by hand, shows Diazyme holds two nine‑digit licences, `081708902` and `081730202`; `0817089` is not a licence key. The error survived seven weeks because it validated itself: `giai:0817089.2` rendered without its dot is `081708902`, the real key. It was found by `td_rederive.py` failing its own positive control and refusing to judge 12,047 claims. Seven rule additions proposed for canon, for ratification: a correction can install the next defect; a source name is a claim that must be true on the day it is written; a self‑validating error is only found by re‑derivation; the separator must travel; no EPC key is ever hand‑written; a control before an experiment, and failing it is fatal; an open ruling is an open wound. Remedy: re‑root `0817089 → 081708902` everywhere with the old value kept as a superseded reading, re‑mint the GIAIs through the engine, add `081730202` as candidate, re‑read the other eight recorded hand‑reads, and capture the VBG screens into the repo.

**2026‑09‑04 evening · [TAPE · the prefix is the base; party was ours](tape/TAPE_2026-09-04_evening_prefix_is_the_base.md).** KJ's ruling, verbatim: a prefix is the primary key and party should never have led anything. Party was minted by the name fold, the most‑corrected operation in the project, then made the parent of the authority's fact through `company_prefix.party_id NOT NULL`; the inference became the spine and the fact became the child. The evidence trail is the same fact in five accents: 4,099 harvest refusals, 6,629 stage refusals, 1,232 held‑elsewhere contests, 312 silent overwrites of the scalar `party.prefix`, and 54,000 rooted rows that KJ identified as GDSN. Item 3 answered: production has no `root_method` column, so how a prefix was rooted is unrecordable there by construction, and the night's 449 claims survive only in `party_event.detail`. The tool's reconcile counted statements, not distinct outcomes. Part C proposes the additive three‑table structure (`prefix`, `prefix_claim`, `prefix_association`) with the promotion gate, rebuilt from stage, never in place; the child‑table re‑keying question is left unresolved and unmeasured. Measured: idempotency observed, the full dry run reconciles at 23,910, the `gln` gate caught five sequential single‑issue keys on party 4148869, the `stage` gate is a candidate for structurally incapable of firing. Five Claude corrections. Part G: the register's claim that `td_gs1_engine.py` does not exist is a scope artefact, the engine lives in the engine repo; Gate 2 is recorded two ways on the same day. IP flag carried. GAS stays at 3.

**2026‑09‑04 · [TAPE · the prefix is the key, and the data said so first](tape/TAPE_2026-09-04.md).** KJ's ruling: every prefix is a primary key with no primary among them, one prefix is one ThingSite, there is no merge and no supersession, the address is the prefix, and no prefix means no ThingSite. A mirror with an opinion is a bad photograph; Claude broke the rule twice on Diazyme's two party rows. Part B records the lesson that the refusals were a ruling, not a backlog: 4,132 refused with length 0 and licence type 0, one gate carrying the whole disagreement, so the new rule is read a gate before you work it. Part C measures the backend as built, not assumed: the engine at 27 PASS, the API live on 8787 with 20 endpoints, the population database at 102,472 records, the fleet self‑test passing, the first promoter at 586 admitted and 4,132 refused; exactly one tool missing, `promote_stage_to_production`, and the failing test suite is a gate working. Part D corrects three git assertions against the platform team's structure document and records the position taken on two repositories. Part E: six screens built and wired, the mark is a graph, three channels carry three facts, copy rulings, and a zero‑width button found by walking the product. Seven open items including the identity count that does not reconcile, the unratified 25 August rows, and GoDaddy's Agent Name Service routed to counsel. GAS stays at 3.

---

## THE CORRECTIONS TAPE — SOLID · do not re‑litigate

*Each was assumed once, corrected, and is now a passing test or a cited source. Do not reason from first principles about these.*

| # | Assumed | True | Enforced by |
|---|---|---|---|
| 1 | GIAI must be numeric | GIAI is character set 82 (alphanumeric) per GenSpecs 25.0 Fig 1.4.8‑1; numeric‑only is a TDS 96‑bit tag constraint, not a GS1 rule | P1–P8 |
| 2 | "GIAI can be alphanumeric — flag it, don't reject it" | Fail‑late. encode↔decode is a lifecycle property; the gate is in `mint()` | P19 |
| 3 | The carrier gate is a GIAI rule | General — binds GIAI, CPID, GDTI alike (a 96‑bit tag packs the variable part as a number) | P22 |
| 4 | Caterpillar→Butterfly is "GS1's own rule, operationalised" | Overstated; §4.5.2 is a SHOULD ("replace… or be removed"). The standard contemplates destroying the provenance spine — we depart from it | P20 |
| 5 | GS1 covers the asset lifecycle | GS1 writes standards; it does not implement them. No GS1 rule for a finished good becoming a component (GIAI→CPID) | P21 |
| 6 | Two GLNs at one address = a shared place | Nothing physically co‑occupies; things NEST. `sameAs` is an evidence‑backed assertion, owned by a named asserter; the registrar never adjudicates it | P27, P28 |
| 7 | Alphanumeric is fine where GenSpecs permits | R9: ThingDaddy‑GENERATED identifiers SHALL be numeric (letters = Legacy ID, digits = minted). GENERATED ≠ ADMITTED | P23, P24 |
| 8 | The lot belongs in the LGTIN | A lot is an ATTRIBUTE. LGTIN is COMPOSED, never minted (`urn:epc:class:lgtin`, class: not id:) | P31, P32 |
| 9 | `EO` is a grounded item→GTIN qualifier in X12 element 235 | **`EO` is ABSENT from X12 4010 element 235.** `UP`/`UK`/`EN`/`UA` ground verbatim; `EO` is a CANDIDATE that failed to ground — stop carrying it as grounded until verified vs 5010/6010/8010 | away‑hunt 2026‑08‑02‑1820 · zenbridge X12 4010 element 235 |
| 10 | EDI element 235 carries a generic "GTIN" | It carries the GTIN **packaging tier**: `UP`=GTIN‑12 (consumer), `EN`=GTIN‑13, `UA`=case, `UK`=GTIN‑14 (shipping container). Record the tier, don't collapse to "GTIN" | away‑hunt 2026‑08‑02‑1820 |
| 11 | Diazyme assay count is ~53 / ~63 | **MK‑034 BOM is authoritative: 51 assays × 169 components.** Use 51 | this session 2026‑08‑02 (GA_Book / MK‑034) |
| 12 | EDI can't be modeled by the Allotrope/SHACL conformance engine (it isn't a lab‑measurement ontology) | REFUTED by running it. EDI is a **pluggable vocabulary of equal standing to AFO**; `meta_configurator.py` validates SEVEN vocabularies on one engine (the Walmart 850 conforms; drop the seller → exception at the exact rule) | N26 · `meta_configurator.py`, `install_edi_ontology.py` |
| 13 | The two "stacks" are a legacy box → Physical‑AI spine | **THREE stacks, one identity:** Semantic Similarity Stack (Tim Berners‑Lee substrate, closed on both axes — identity INV‑9, vocabulary INV‑10) · Vocabulary Funnel (the resolver, Language Architecture) · Physical AI Stack (act, ThingDaddy keystone **centered**, plan above / execute below). 2 movements to pitch, 3 stacks to build. Do NOT relabel the funnel as the Semantic Similarity Stack | this session · `stacks_decision_three_not_two_2026-08-02.md` |
| 14 | The GA→Diazyme discovery walks child‑prefix *up* to the parent | **Inverted.** The real walk is GA (named, GEPIR‑empty) → *down* into divisions → domain prior (med instruments→FDA→UDI→GTIN) → Diazyme → GEPIR returns 0817089. `build0_ga_replay.py` seeds at 0817089 and walks up — backwards; reverse it, and forbid `Diazyme`/`0817089` in its input | `00_SESSION_INDEX_2026‑08‑04` · Build 0 |
| 15 | "CPI vs CPID" is a naming slip to correct one way | **Documented GS1 CROSS‑STANDARD name conflict — NOT an error.** The KEY name is **CPID** (GenSpecs 26.0 "Areas of GS1 system application"); the EPC/AI ENCODING name is **CPI** (EPC TDS 1.13 — AI 8010+8011, `urn:epc:id:cpi`, `urn:epc:tag:cpi-96`, via gs1.org/services/epc-encoderdecoder). Same key, two GS1 standards. **Convention: label CPID, URN `cpi`.** A `cpi:` URN is CORRECT — never "fix" it to `cpid:`. Only a bare **CPI used as the human key label** takes the house CPID form (Book_4 p.27, Caterpillar WPs, Culligan deck, incorporation graphic) | GenSpecs 26.0 + EPC TDS 1.13 · `gs1_key_types.json` |
| 16 | A GTIN band that clears the agreement threshold (≥2 DIs) means the company is single‑prefix — a false‑multi "recovered" | **Over‑recovery — a fabrication‑adjacent error.** A lone qualifying band silently ignores the company's other DIs sitting in *different* bands. On thin evidence FGX (bands 0887661 / 0193033×2 / 0031568) and Cardinal (6 distinct bands) were mis‑called `recovered‑single`. A clean recovery requires **every** foldable DI to share the region (`lcp ≥ min_gcp`) or one band to hold **all** of them; a lone non‑dominant band is **true‑multi** (→ atlas + LEI), a dominant band + orphan tail is **provisional** (confirm on the company's full DI set) — never a recovery. Composes A5/A6: multi‑prefix is not a count you infer from a 2‑DI coincidence. | coverage‑gated `fold_company` (`atlas_resolve.py`) · live DeviceRecipe run 2026‑08‑11 (FGX/Cardinal flip recovered‑single → true‑multi; headline 6.5:1 → honest) · **N28** |

**Meta‑rule:** GenSpecs says what is *permissible*; ThingDaddy says what is *issuable*. A registrar may be stricter than the standard, never looser. Absence returns an exception, never null — a null is a hole a model will fill. **GS1 itself is not internally consistent** (see #15): a KEY may carry one name in GenSpecs and another in the EPC/AI encoding — record both, cite the standard, let the GenSpecs name govern the human label and the TDS scheme govern the URN.

---

## JOURNAL — append‑only, newest first

### 2026‑08‑11 · Session — DeviceRecipe consolidated (3 lanes) + coverage‑gated atlas fold + full‑anchor reparse wired
- **N28 — the multi‑prefix fold OVER‑RECOVERED on thin evidence** (now corrections‑tape #16). The first inline fold declared a company `single` whenever exactly one GTIN band cleared the ≥2 agreement threshold — ignoring the DIs sitting in other bands. On a live 200‑DI openFDA slice this mis‑called FGX INTERNATIONAL (4 DIs across 3 distinct bands) and Cardinal Health 200 (6 bands) as `recovered‑single`, inflating the recovered:atlas headline to a false **6.5:1**. **Fix (coverage gate in `fold_company`):** a clean `recovered‑single` requires every DI to share the region (`lcp ≥ min_gcp`) or one band to hold ALL of them; a lone non‑dominant band → `true‑multi` (atlas + LEI); a dominant band + orphan tail → `provisional` (confirm on the full DI set). Post‑gate live run: FGX/Cardinal correctly `true‑multi`; genuine recoveries (Covidien, 3M, Owens & Minor — all `lcp‑all‑share`, cov=1.00) survive; the headline drops to an honest number. One source of truth: the gate lives in `atlas_resolve.py`, so it fixes both the recipe settle pass and the standalone atlas at once.
- **N29 — `n_distinct_prefix_regions` was never actually computed — it is the literal placeholder `PENDING_FULL_REPARSE` for all 1,934 reopened multi‑prefix rows** (`gudid_multiprefix_reopened_enriched.csv`). The MO extraction produced the consensus *verdict* (multi‑prefix vs prefix‑candidate) but deferred the real full‑DI region count. `batch_fold.py` runs that pass: for each reopened company it pulls up to N of that company's **real** DIs from openFDA (thousands, not the 5‑DI thin sample) and folds them coverage‑gated → `recovered‑single | provisional | true‑multi | thin`, retiring the placeholder with a sourced disposition and producing the civilization‑wide recovered:atlas ratio that actually sizes the LEI/GEPIR workload. This is the **A5 consequence** ("re‑rank the 1,934 multi‑prefix rows") made runnable. Runs `--live` on the Mac (openFDA is cloud‑provenance‑gated, A10); proven end‑to‑end offline in‑container (resumable, incremental flush, skip‑done on rerun). *The thin‑sample 200‑DI ratio (post‑gate) is NOT the civilization number — 64/79 MODIFIED companies were single‑DI in the slice; the real number comes only from the full per‑company DI sets `batch_fold.py` pulls.*
- **BUILT (cloud plane; delivered to Mac `thingdaddy-gh/population/`, live‑confirmed):** (1) **consolidated `DeviceRecipe`** (`recipe_device.py`) — one recipe, three issuing‑agency lanes on the fleet `root→build→grade` contract: **GS1** (MO consensus → `GS1Authority.resolve` → VERIFIED/STUB/CANDIDATE, multi‑prefix → atlas) · **HIBCC** (HIBC `+` → LIC labeler code, own authority, GS1‑crosswalk candidate) · **ICCBBA** (ISBT‑128, own authority); four‑verdict routing **AUTO/MODIFIED/ROOTING/EXCEPTION**; classified by the authoritative openFDA `issuing_agency` field, never guessed from DI shape. Live 200‑DI run: **AUTO 24% (all STUB — no RECORDED authority file yet, honest) / MODIFIED 61% / ROOTING 15% (all HIBCC) / EXCEPTION 0**. Finding: same company issues under two agencies (ARTHREX, SPINECRAFT appear GS1 *and* HIBCC) — split correctly by the authority field; atlas needs a cross‑lane unify. (2) **GCP‑native family merge** in `gleif_atlas.py` (`merge_families`) — merge signals in trust order **shared verified GCP (GS1‑native) > same LEI / GLEIF ultimate‑parent (partial coverage) > separate candidate**; AVID name‑variants merge on shared GCP 0809160 with **no LEI needed**, Cardinal 200 / ICU pull their **real** LEIs (`5493008FGLV025KJOS48`, `5493003NRW7LS2QZRH60`) — no forced merge, nothing fabricated.
- **GAS stays at 3.** No new philosophy opened — this session was consolidation + a correction (the coverage gate) + retiring a deferred placeholder.

### 2026‑08‑04 (later) · GS1 key‑type model grounded to the standards + CPID/CPI conflict resolved
- **Canonical key‑type reference built** (`ThingDaddy_Prototype/gs1_key_types.json`): all nine ThingDaddy keys + six other GS1 keys, each with its **GenSpecs 26.0** name, its **AI(s)**, and its **EPC TDS 1.13** URN scheme, every field tagged grounded vs candidate. Grounded on GS1 GenSpecs 26.0 (Ratified Jan 2026) + the GS1 EPC Encoder/Decoder (TDS 1.13). KJ‑sourced screenshots 2026‑08‑04.
- **CPID/CPI resolved as a documented GS1 cross‑standard conflict** (tape #15 rewritten): KEY = CPID (GenSpecs 26.0); ENCODING = CPI / `urn:epc:id:cpi` / `urn:epc:tag:cpi-96` (TDS 1.13, AI 8010+8011). Convention = label CPID, URN `cpi`. The T5 language check was corrected: a `cpi:` URN and AI 8010 are CORRECT and must not be flagged; only a bare CPI **key label** is a house‑style note (not fabrication).
- **Candidates still owed a GenSpecs section lookup** (not asserted): party‑GLN AI (417), the GTIN trade‑item name row, LGTIN lot AI (10) / composed URN. The full GenSpecs 26.0 PDF is now in hand for these.

### 2026‑08‑04 · Session — FOUNDER GATE ratified (A1–A15, T7, F10, renderer items) · book renderer proven · project cleaned

**RATIFIED (R4 · KJ, this session):**
- **Rules A1–A15 — all fifteen ratified.** (A1 prefix is the root not the qualifier · A2 unrooted≠unqualified · A3 value and latency are separate axes, never summed · A4 the three axes are independent · A5 multi‑prefix≠large enterprise · A6 prefix length is not a pattern · A7 a prefix in our own doc is not verified · A8 a failed call is never an absence · A9 buy the sanctioned channel, never automate around a block · A10 scheduled cloud cannot fetch — cloud locates, Mac grounds · A11 build the whole network, value is N² · A12 measure the unit cost before scale · A13 the vocabulary pack is the unit of leverage · A14 a job posting is a vocabulary manifest · A15 the authority record has no logical half). Evidence per rule in `claude_EXTRACT_rules_vocabulary_model_capture_loop_2026‑08‑04.md`. **Consequences accepted:** A5 reopens the **1,934** excluded multi‑prefix rows (Mac re‑rank by device count AND prefix count); A9 opens the **GS1 US Data Hub** licence conversation; A11 commits to **no scoring gate** — build them all, distinctness measured, never gated.
- **T7 distinctness threshold = 25%, PROVISIONAL** — validated against the three real unrooted Mac picks before locking (A12; do not lock in the abstract). **Cloud measurement 2026‑08‑04:** on two FULL‑LENGTH books (100 pp), whole‑book distinctness is **3.1%** (the ~80‑pp FIXED walk swamps it) and variable‑front‑matter distinctness is **16%** — both below 25%, and this is the rooted‑vs‑unrooted CEILING. Implication: measure T7 on the VARIABLE content only, enrich the front on the Mac (full GUDID + sitemap), and recalibrate the threshold against real thin‑unrooted picks. The FIXED walk may need to be a shared companion volume.
- **F10 Diazyme proof chapter = YES in every book, neutral framing** — "one company, already running"; never named as a GA division in non‑GA books.
- **Renderer open items — ratified:** (1) gap cost‑model factors **mass / regulatory load / custody churn**, reported separately, never summed (honors A3); (2) a first‑party signal MAY render **Pillar‑4 candidate bindings**, cited, prefix slot stays dark; (3) the **bow omits** when there is no cited AI‑programme evidence (never "no programme found").

**BUILT (cloud plane, no fetch):**
- **The FIXED spine** `claude_SPINE_thingsite_book_FIXED_v0.1_2026‑08‑03.md` + **the TEMPLATED sections & gap chapter** `claude_SPINE_thingsite_book_TEMPLATED_and_gap_v0.1_2026‑08‑04.md` + the **vocabulary packs** (`ThingDaddy_Prototype/vocabulary_packs.json` — 9 packs across 4 layers; A13) + the **GS1 key‑type reference** (`gs1_key_types.json`).
- **Prototype renderer** — v1 (projection proof) → v2 (**pack‑aware**: pillars chosen by the entity's stack) → v3 (**full‑length**: complete FIXED doctrine + the annotated screen walk + the per‑company front). Two full books rendered (Diazyme 100 pp / GA 101 pp). **Acceptance suite `thingsite_book_tests.py`** (T1–T10) run: T1/T3/T4/T5/T6/T8/T9 green, T2/T10 need Mac artifacts, T7 as above. Books kept internal (wall §7). Production run is **Mac** (A10).

**CORRECTIONS logged (from `00_SESSION_INDEX_2026‑08‑04`; see also tape #14, #15):**
1. **GA→Diazyme arrow inverted** in two artifacts — reverse Build 0's walk; forbid `Diazyme`/`0817089` in its input; GLEIF may be the wrong source (GA private, L2 thin on private parents). *(tape #14)*
2. **Unrooted is not unqualified** — the prefix is the root, not the qualifier (A1/A2).
3. **Value and latency are separate axes, never summed** (A3).
4. **Multi‑prefix ≠ large enterprise** (A4/A5); the 1,934 exclusion was a method limitation, not a quality judgment — second time in one session a filter was deleting the best targets.
5. **Scheduled cloud cannot WebFetch** — cloud locates, Mac grounds (A10).
6. **GS1 US Data Hub has a name‑search API + downloadable prefix file** — closes **H1**; the earlier "GEPIR has no automated route" assertion was wrong (A9).
7. **The accountant rule:** answer structure from knowledge, current state from a source, and never let them carry the same confidence.
8. **CPID/CPI is a GS1 cross‑standard name conflict**, now grounded (GenSpecs 26.0 = CPID; EPC TDS 1.13 = CPI) — label CPID, URN `cpi`; do not "fix" a legitimate `cpi:` URN. *(tape #15)*

**VERIFIED FACTS (authority‑checked this session):**
- **Culligan International Company** · Rosemont IL · prefix **0033663** · GLN **0033663000001** · GS1 US. → The Culligan deck + SDK doc were built on **0071878 — WRONG; re‑root to 0033663** (A7).
- Culligan licence keys: **San Paso 0029951** · **Wichita 0076143** · **Quad Cities 0728372**.
- **Caterpillar Inc.** Peoria · **0850063486** · GLN 0850063486002 (updated 2025‑05‑25); Deerfield · **081568302** · GLN 0815683020005 (updated 2023‑04‑23). Multi‑prefix single enterprise (A4).
- **Every Verified by GS1 record pulled shows Website: Unknown — 4 of 4** (A15).
- Name‑match counter‑examples (kept honest): **Cozy Caterpillar** 081467300 · **The Calm Caterpillar** (3 prefixes, Cincinnati) — small business, multiple prefixes (A4/A5).

**Project hygiene:** pruned ~80 `shadow_away_hunt` docs (≤2026‑08‑02) + superseded one‑off graphics + old doc versions; **away‑hunt cadence 3h → 12h** (`0 */12`); nightly sweep kept. Nine session artifacts committed to knowledge; `physical_ai_stack_incorporation` graphic delivered (project rejects image writes — KJ to attach in app). Shadow‑doc grounding carry (reclaim runbook Steps 1–3) deferred to the **Mac** — copies are local, nothing lost.

**GAS stays at 3.** The gate closed open questions; nothing here opened new philosophy.

### 2026‑08‑03 · Session — Physical‑AI action layer BUILT (verdict · trilogy · OT sim) + 3 stacks + IP v3
- **Live authorization verdict (H‑1) BUILT** in the prototype (P5): resolve identity envelope → five gate checks → **APPROVED · CONSTRAINED · HUMAN · DENIED**, four grounded Diazyme scenarios (BHB run → APPROVED; HCY QC‑lapse → CONSTRAINED; firmware‑release → HUMAN; unknown reagent lot → DENIED, "no resolved identity, no verdict, no motion"). The Physical‑AI keystone turned from model to clickable RTP. Grounded IDs only (giai:0817089.1/.2, gtin:00817089023529, sgln:0817089.020009); HCY GTIN left candidate (not fabricated).
- **Physical‑AI trilogy folded into P5:** the **real GUDID civilization graph** (7,102 companies · 1,491,123 devices · 68 nations, Diazyme green — pulled from the existing `digital-civilization-commons` artifact, NOT rebuilt), the **37‑agent GSRN workforce** (from `grounded-agents-value-pillar`), and the verdict — the pillar now walks graph → MCP → agents → verdict end to end.
- **OT tile BUILT (R3):** working `asyncua` OPC‑UA server where **NodeId ≡ GIAI** (`ns=2;s=giai:0817089.2`), read back live over `opc.tcp` with GIAI‑rooted variables (cal/QC/temp/last‑assay). `ThingDaddy_Prototype/opcua_giai_sim.py` + console evidence (`graphics/opcua_giai_evidence.png`). R3 candidate·buildable → **built · sim**; R4 Kepware→SiteWise Edge stays **model** (AWS config, ≤100 servers/gateway) above the proven server side.
- **Three stacks ratified** (correction #13) + **integration graphic** ("two stacks, one identity — they meet at the graph"); the wrong gs1net3 "legacy box" stack deleted. Green ThingDaddy keystone moved to the **center** of the Physical AI stack (plan above / execute below). Graphics: `semantic_similarity_stack` · `vocabulary_funnel` · `physical_ai_stack` · `two_stacks_integration`.
- **IP disclosure v3** (`claude/IP_disclosure_package_v3_2026‑08‑03.md`, delivered .docx): adds **§H Physical‑AI action layer** (H‑1 verdict keystone, H‑2 grounded agent workforce, H‑3 sim‑to‑real, H‑4 signed context package, H‑5 edge trust agent, H‑6 governance policy suite), **§I lifecycle‑invariant standing** (I‑1 erasure↔immutability via person≠thing+plane separation, I‑2 sanctions‑as‑revocable‑edge OFAC‑validated, I‑3 standing⊥existence), **A‑5** X12↔EDIFACT carrier‑equivalence with tier‑recovery. Candidate; counsel constitutes the register; H‑1 proposed as new keystone.
- **Master doc** (`ThingDaddy_Diazyme_GA_Master.docx`) rebuilt with the three stacks + integration + the civilization graph (p.44) + the agent‑workforce/verdict expansion. Preview "Failed to load" at 11.6 MB → compressed to **7 MB** (big graphs → JPEG q82, screen panels → JPEG q80).

### 2026‑08‑02 · Session — the ThingSite prototype, the ledger, and the books (DECISIONS)
- **Built the clickable Diazyme ThingSite prototype** (`ThingDaddy_Prototype/thingdaddy.html`): five pillars + Key types (nine slots) + per‑pillar What/How/Why education + the **source↔chip** inspection (tap a claim → its human source PDF beside the machine‑readable in‑context artifact, gate‑checked). Chips live: SiLA on **both** analyzers (c270→MK223 catalog, 3000 Plus→D136 brochure), five AFO assay methods (real GDTIs + GTIN‑rooted BOMs, conformance 17/17), the 850 (harvested EDI guide → raw X12 → rooted GLN/GTIN), **OPC‑UA** (LADS OPC 30500 → NodeId ≡ `giai:0817089.2`), MTConnect stub (GA‑side, candidate).
- **Added two views:** *The Ledger* (append‑only, GSRN‑stamped, ruleset‑versioned, evidence‑attached, replayable, "checkable including against us"; `node_event`/`edge_asserted` store built, signing/watermark/canary designed) and *GA Requirements & Compliance* (R1/R2 real‑captured, R3/R5 buildable, R4/R6 model; DoD expression honestly graded).
- **Decisions/framing:** MK‑034 = **51 assays** authoritative (correction #11). **CMMC Phase II badged suspended** (60‑day review) while DFARS 252.204‑7012 / NIST 800‑171 / SPRS remain in force and IUID/CUI provenance is durable — verified current via web search + the nightly watcher.
- **Deliverables:** both GA books updated (`…_UPDATED_2026‑08‑02`), **IP disclosure v2** (`claude/IP_disclosure_package_v2_2026‑08‑02.md` — adds the identity‑rooted provenance ledger, source↔chip conversion, MCP grounding interface, identity‑rooted conformance engine as new candidate facets; maps to the IP LOG; no TD‑M numbers invented). Screens doc (22 screens folded in) + print layout. All candidate/graded; nothing minted; R4 intact.

### 2026‑08‑02 · Nightly sweep (20:0X) — FSMA‑204 fourth boundary TYPE
- **Authority‑lookup exemption** discovered as a new FTL boundary type: the line can run through an **external authority listing**, not just intrinsic class. BelGioioso cheese — fresh/soft ON, aged/hard OFF (moisture/aging class), **but IMS‑listed Grade A cottage cheese EXEMPTED** (Interstate Milk Shippers oversight). ThingDaddy's verify‑or‑exception law appearing inside the regulation.
- **11 candidate GTINs** (mod‑10 re‑checked in‑cloud, 0 rejects, prefix=NULL) across two runs `031142` (retail) / `753340` (foodservice) = **fourth consecutive food node with the multi‑prefix atlas (TD‑M‑51) signal**. Reseller code `731338878855` held OUT (mod‑10 valid ≠ brand‑issued).
- Prospecting: live **June‑2026 soft‑cheese Listeria outbreak** (FDA+CDC, recall expanding) validates the node. ThingSite draft delivered. → Ratify **R‑15**.

### 2026‑08‑02 · Away‑hunt (18:20) — EDI cross‑syntax carrier layer grounded on BOTH sides
- **X12 warehouse docs grounded:** 940 `W01` line loop; 945 `W12` item detail + `MAN`/`GM`→SSCC; element‑235 packaging‑tier qualifiers (`UP`/`UK`/`EN`/`UA`).
- **EDIFACT/EANCOM twin grounded verbatim** (GS1 DESADV D.01B): `NAD`+`3055`=9→GLN, `LIN`/`C212`/`7143`=`SRV`→GTIN, `GIN`/`7405`=`BJ`(+`PCI`/`33E`)→SSCC. The reversible 856↔EPCIS transform IP flag now has a grounded X12↔EDIFACT carrier‑equivalence table under it (see A‑5, IP v3).
- Corrections #9 (`EO` fails to ground) and #10 (element 235 = GTIN *tier*) captured. **Counsel seam:** lossless X12→EDIFACT round‑trip needs tier‑recovery from the GTIN indicator digit (wrinkle on the 0616 reversible‑transform flag).

### 2026‑08‑01 → 07‑31 · Food‑pole boundary series (carried into this reconstruction)
- **Taylor Farms** — leafy‑greens pole exemplar (source publicly named; multi‑state recall). → R‑12.
- **Vital Farms** — shell‑egg node, **item‑type boundary** (eggs ON / butter OFF); two‑run atlas `861745`/`862315`; reinforced by the July shell‑egg outbreak. → R‑13.
- **Trident Seafoods** — seafood/finfish node, **processing‑state boundary** (raw cod ON → breaded/battered/canned → surimi transform); two‑run atlas `028029`/`071960`. → R‑14.
- CBV event vocabularies grounded at token level (`bizStep`, `disposition`) — completing the four‑vocabulary EPCIS triad (away‑hunt 1514).
- **Food‑pole net:** four distinct FTL boundary TYPES now demonstrated — uniform → item‑type → processing‑state → authority‑lookup — each a food node carrying the two‑run multi‑prefix atlas signal.

### 2026‑07‑29 · Engine / graph corrections (from TAPE_APPEND v2)
- **N25** — every write rewritten to the real schema after read‑only `\d` inspection; `node_gate_prefix CHECK (state<>'verified' OR prefix NOT NULL)` enforces the gate in the DB. *Look before you cut.*
- **N26** — EDI is a pluggable vocabulary of equal standing to AFO; SEVEN vocabularies on one engine (see correction #12).
- **N27** — NULL‑legacy jsonb trap: `NOT (NULL ? 'k')` = NULL; every jsonb `?` guard needs `COALESCE(legacy,'{}'::jsonb)` first.
- **Diazyme threaded to SOLID:** HCY = first complete voice (all 5 pillars + SAP IDoc bindings MATMAS05/LOIPRO05/DOCMAS04/QALITY02, bound to real instrument node `giai:0817089.1002`); Fibrinogen = second voice (template replicates); **41 bare assays** marked `pillars_pending`; **GTIN spine honest at 222**; GA candidate overlay live (prefix NULL, never Diazyme's). *3 threaded voices, 41 honest gaps, zero fabrication.*

---

## WATCHER STATE — 2026‑08‑02 (no movement unless noted)
- **CMMC Phase II** — suspended, inside the 60‑day reform review (launched ~Jul 20; window to ~Sept 18). DFARS 252.204‑7012 / NIST 800‑171 / SPRS remain in force. Affects GIAI/CPID provenance framing.
- **FSMA‑204** — Q&A guidance + IMS cottage‑cheese exemption + quarterly listening sessions (early‑2026 dated; newly in watcher scope). **July 20, 2028** compliance date and **GTIN + lot + GLN** KDEs unchanged.
- **GS1 GenSpecs — Release 26.0 (Ratified Jan 2026)** in hand (KJ, full PDF). No key‑type or minting change; five corrections survive; the CPID key name is confirmed here. (R‑2 recommend CLOSE.) **EPC TDS 1.13** confirmed as the URN/tag encoding standard (CPI / urn:epc:id:cpi).
- **MCP** — `2026‑07‑28` still current; no `2026‑08‑xx`. Read/propose face, not a mint gate. (R‑3 recommend CLOSE.)
- **FDA UDI / 21 CFR 830 · SiLA 2 · Allotrope AFO · OPC‑UA · AWS/Azure IoT · DOE/NRC** — no version/identity change this window.

## RATIFY QUEUE (KJ · R4) — carried
Rules **A1–A15 RATIFIED 2026‑08‑04** (moved to the journal). · R‑15 BelGioioso cheese node (draft delivered) · R‑13/R‑14 reinforced by live outbreaks · R‑12 Taylor Farms · R‑11→R‑4 exemplar ThingSites/atlas · R‑3 & R‑2 **recommend CLOSE** · R‑1 Samsung GTIN `8806088031644` (pending since 07‑19).

## OWED / CARRIED TO THE MAC (auth/provenance‑gated from cloud)
Prefix resolution (GEPIR / Verify‑by‑GS1) for every candidate prefix run; **rotation cursor: Labcorp next**, then CVS/Walgreens/ODFL + Diazyme openFDA records 100–184 + Honeywell/Thermo/Illumina catalog→GUDID; UNECE `tred7143`/`tred3055` verbatim + resolve `EO` against a specific X12 version; CBV 1.1.0 cardinality / 2.0 delta; DZ‑Lite Host/LIS interface manual (ASTM vs HL7); seven Diazyme→LOINC assignments; GLEIF LEI tree. **Carried 2026‑08‑04:** (a) **reverse Build 0's walk** (GA→down→Diazyme) + forbid `Diazyme`/`0817089` in input + add non‑GLEIF child sources (company site, FDA labeler); (b) **re‑root the Culligan deck + SDK doc 0071878 → 0033663**; (c) **re‑rank the 1,934 multi‑prefix rows** by device count AND prefix count (A5) — **now runnable: `batch_fold.py --live` retires `PENDING_FULL_REPARSE` with the coverage‑gated full‑DI region count (N29)**; (d) run the **three‑pick selection query** + render + **validate T7** on the VARIABLE content (whole‑book/front‑matter both measured 3.1% / 16% on the cloud ceiling pair — recalibrate on real thin‑unrooted picks); (e) apply the **CPID label / `cpi` URN convention** in Book_4 p.27, Caterpillar white papers, Culligan deck, and the incorporation graphic — relabel the human KEY as CPID; **leave `cpi:` URNs as‑is** (documented GS1 conflict, `gs1_key_types.json`); (f) **grep‑and‑carry the shadow docs** on disk (reclaim runbook Steps 1–3) before they age out; (g) the **Data Hub** licence conversation with GS1 US (A9); (h) promote the three candidate key‑type facts (party‑GLN AI 417, GTIN name row, LGTIN lot AI 10) against GenSpecs 26.0. **New 2026‑08‑11:** run `batch_fold.py --live` over the full 1,934 reopened set (openFDA, up to N DIs/company) to produce the civilization‑wide recovered:atlas ratio and write real dispositions in place of `PENDING_FULL_REPARSE`; carry the **cross‑lane unify** (one company issuing under both GS1 and HIBCC — ARTHREX, SPINECRAFT) into the atlas; swap the real RECORDED authority file in so AUTO grades can reach VERIFIED (STUB today). Files delivered to `thingdaddy-gh/population/`: `recipe_device.py`, `atlas_resolve.py`, `gleif_atlas.py`, `batch_fold.py`. Prior: align canonical arch doc §1 (still says ThingDaddy = "Layer 3") to the centered‑keystone decision; capture a static full‑bloom of the animated Diazyme graph if a settled end‑state exists; wire the OPC‑UA sim to a real SiteWise Edge gateway when an AWS account is available (turns R4 model → built).

---

## THE COMMIT RITUAL (keep this record alive)
At the end of any session that produced a **correction or a decision**: (1) add a journal entry here — assumed / true / enforced‑by; (2) amend the corrections tape if a new correction belongs; (3) if code changed, run the three reference files and regenerate the mirror; (4) bump State v in `00_READ_ME_FIRST`. Newest on top. Prepend the Mac originals (N1..N27) when located; never overwrite.

*The tape gives truth instead of performance. The journal lets the record speak for itself. Neither of us has to be trusted; both of us are checkable.*
