# PR 27 — day/2026-09-06 → main

`git log main..day/2026-09-06 --first-parent`, one line per commit, grouped by block.
Merges: w2/promote-harvest-to-stage (×3), w3/site-truth (×2), w4/thingsite-banner (×2), origin/main (×1).

## stabilise

- `1a8605f` tools: promote_stage_to_production.py — the stage→production promoter, committed as found. Wrote 448 rows to thingdaddy_population on 4 Sep; reconcile counts statements not outcomes, so 448 admitted = 137 distinct roots.
- `a7fec04` seed: disposable local dev database — 4 parties, 187 claims. Dated snapshot of real production rows; regenerate with make_seed.sh, never edit.
- `986afdb` seed: demo_up.sh / demo_down.sh — one command to a working demo. Asks every route the screens call; refuses READY if any is missing. Closing count read from the attached DB: seed 4, production 102,472.
- `8925c1b` console: td_console.html, 26 screens. Tier 1 (1–17) fed by real endpoints; Tier 2 (18–26) demo shells behind a red banner. server.js gains 7 read-only routes (/containment, /exceptions, /estate, /claims/grades, /party/:id/claims, /prefix/:prefix/claims). /containment surfaces MedFare 081708901 inside 0817089.
- `29a4faa` tools: td_audit.py (17 checks; first run 14 findings, 0 never fired), td_rederive.py (positive control failed → refused to judge; found a wrong demo root standing since 22 July), td_authority.py + authority_reads.json + 2 captures (2 confirmed, 8 unverified transcriptions). Verified by GS1, 5 Sep: Diazyme holds 081708902 and 081730202; 0817089 is not a licence key. td_thingsite.html added with -f past the *_ThingSite.html ignore rule.
- `5047b4b` stabilise: td_demo, td_screens_live, td_thingsite tracked; .gitignore narrowed to generated output; mirror_claims.json + td_mirror.py in; ecosystem endpoint in server.js; audit A4.

## schema

- `b6752c5` migrations/012_constitution.sql: accounting_role · authority · ruleset_version · actor NOT NULL (validated, 0 fail); verified-has-urn (NOT VALID, 20 fail, listed in file).
- `c25a545` migrations/013_run_party_stage_ancestor.sql: party.stage_ancestor on the run, nullable. Not applied; KJ applies. promote_harvest_to_stage.py exits 20 until it is.

## promote

### DB2→DB3 dry run (stage→production)

- `34864f9` docs/promote/dry_2026-09-06.txt: full-volume dry run — 15,502 admitted, 8,270 refused, 6,958 of the refusals the scalar party.prefix column.

### DB1→DB2 promoter (harvest→stage)

- `a52236d` tools: promote_harvest_to_stage.py — DRY by default, --apply requires --i-am-kj, exit 0/10/20/30. Source harvest_company grade='rooted'; target run party + company_prefix. Five gates: g1 length 4–12, g2 licence_type read not inferred, g3 GLN roots in the prefix, g4 one prefix one row across both carriers, g5 key types and grades in vocabulary. Reconciles on distinct outcomes, rolls back otherwise.
- `1438c5f` dry run, demetech 6 Sep, on a disposable clone with 013 applied — refused licence/licence_type-not-read, also prefix/held-elsewhere. 0652927 is on party 4081962 (party.prefix) and 4148581 (company_prefix): two holders, KJ's to resolve.
- `edb2080` g2 reads the licence from the regulators — GUDID issuing_agency, NMPA cpbsbmtxmc — joined prefix → device → labeler, every device on the labeler counted by agency. All GS1 → licence_type 'GS1 Company Prefix', source 'regulator:GUDID'/'regulator:NMPA', KJ ruling 2026-08-30 cited. Mixed → licence/mixed-issuing-agency with counts. Two indexed steps per register: 22 s → 0.02 s a company.
- `1a35cb9` dry runs 6 Sep against the real thingdaddy_run (013 applied there). demetech: GS1 from GUDID (13,903 devices), then refused prefix/held-elsewhere. Sample 300: 0 admitted, 294 licence not read, 6 held-elsewhere. Mixed: 0192629 (HIBCC 17,290 / GS1 6,068), 4547410 (GS1 468 / MA-code 2).

### GUDID register

- `63d3298` --register gudid — gudid_company_prefix × gudid_company, one staging row per (duns, prefix), keyed gudid:<duns>. g4 refuses n_companies_on_prefix > 1 as prefix/shared-on-register and a second prefix for a party already admitted in the queue as party/holds-other-prefix. company_prefix records register, source_di, n_devices. Default register stays harvest_company.
- `17374c4` --register gudid --limit 300 dry, 6 Sep: ADMITTED 89 · UNCHANGED 5 · REFUSED 206 (licence 40 mixed-issuing-agency · prefix 140 = held-elsewhere 67 + shared-on-register 73 · party 26 holds-other-prefix).

## site

- `ce38fd6` site-truth(td_home): every visible GEPIR reference → Verified by GS1; 081708902 supersedes 0817089 wherever presented as Diazyme's prefix (composed keys, GLN 0817089020009, GTIN 00817089023529, AWS Thing name untouched); supersession note dated 5 Sep; td_home.html now tracked. 11,144,241 not in this file.
- `76b3efa` demo_up: SCREENS check after ROUTES — six screens present or MISSING; any MISSING kills the API and exits 1 before READY.
- `d58d702` screens_live: register count from /stats.total ("N records in the register"); the 11,144,241 identities and 27,787 entities fixture totals removed. No API → "register offline — no count to show".

## thingsite

- `fb1930a` td_thingsite.html reads ../standards/authority_reads.json: superseded[] prefix → red banner + delegation card NOT CONFIRMED; reads[] confirmed → CONFIRMED with read_at and capture; else unchanged. td_console View ThingSite opens td_thingsite.html#<prefix> in its own window.
- `bc566ad` ThingSite: authority banner rebased onto the newer front-door page (empty-hash front door, reveal() at #q/<name>, licence switcher, New here? block). Not-found card now consults the register too. Rendered headless: empty hash, #0817089, #081708902, #q/diazyme, #0614141.

## audit

- `929d411` td_audit B4: every rooted GUDID prefix in production checked for a DB1 gudid_company_prefix row with root_method and source_di. First run: 138 checked, 137 with, 1 without (0817089). cannot_check entry corrected: issuing_agency is stored in DB1; what is missing is the crossing (DB2 source_di filled on 3,659 of 18,472 gudid rows; DB3 has no company_prefix). Runner gains `feed`, `detail`, HARVEST_DB.

## shimadzu (P1/P3 content)

- `98bb731` feat(shimadzu): seven P1 claims from 228-97194D p.281 — the CMD is a carrier the maker means to re-point (reinitialised and reused on a new column). Saleable number left as an exception (228-37281-41 vs S228-92943). w1_hand_acquire_queue.csv: four documents an agent cannot fetch.
- `7483e16` fix(p3): "P3 v ZERO" docstring corrected — 31 P3 rows carry 'v' (Shimadzu 4151801, 3 Sep); gate check rescoped to the AFO block, still 10/10; grade vocabulary is CHECK-enforced. 228-97538 row narrowed to the 2D payload only.

## tape

- `b8e8ec8` docs/tape/: 4 Sep, 4 Sep evening, 5 Sep, LESSON crash-test dummy #2 filed whole; phase log gains one pointer paragraph per file. TAPE_2026-09-06.md not on this machine; not filed.
- `a834eea` (via origin/main merge `42be011`) docs: 00_THE_CANON.md + THINGDADDY_PHASE_LOG.md lifted verbatim from docs/the-record; the three td-* agent files updated.

## NOT in this PR

- the Diazyme re-root
- the plane ruling
- migrate_to_real
- ACME

## for Pom

- review
- run `population/seed/demo_up.sh`
- the g2 register question: g2 now reads licence_type from the regulators (GUDID issuing_agency / NMPA cpbsbmtxmc); default register stays harvest_company; 40 of 300 GUDID rows refused mixed-issuing-agency.
