# Core loop runs on the Python standard library (sqlite3, urllib, csv, json, difflib).
PyYAML>=6.0        # config.yaml parsing (required for live runs; offline falls back to defaults)
openpyxl>=3.1      # optional: xlsx export (CSV always written)
ijson>=3.2         # optional: streaming parse of large GLEIF Golden Copy files
