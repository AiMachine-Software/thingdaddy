{
  "name": "thingdaddy-population-api",
  "version": "1.0.0",
  "private": true,
  "description": "ThingDaddy Population Database — REST API service (Step 3). Isolated data tier over the thingdaddy_population Postgres DB. Does not touch platform/.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
