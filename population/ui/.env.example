# Copy to ui/.env (git-ignored). Point the UI at the population API and, for the
# write actions (register / claim), supply the SAME token the API was started
# with (INGEST_TOKEN in api/.env). Leave the token unset to run the UI read-only.
VITE_API_BASE=http://localhost:8787
VITE_INGEST_TOKEN=
